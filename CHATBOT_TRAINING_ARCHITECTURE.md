# 🏗️ Chatbot Training System - Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                   SWEKEER CHATBOT SYSTEM                    │
└─────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────┐
    │              WEB INTERFACE LAYER                      │
    ├──────────────────────────────────────────────────────┤
    │  /chatbot/training/  ← Training Page                  │
    │  /admin/             ← Django Admin                   │
    │  /chatbot_response/  ← Chatbot API                    │
    └──────────────────────────────────────────────────────┘
                           ↓
    ┌──────────────────────────────────────────────────────┐
    │              VIEWS & CONTROLLERS                      │
    ├──────────────────────────────────────────────────────┤
    │  chatbot_training()  - View/List                      │
    │  edit_training()     - Edit pairs                     │
    │  delete_training()   - Delete pairs                   │
    │  chatbot_response()  - Handle user questions          │
    └──────────────────────────────────────────────────────┘
                           ↓
    ┌──────────────────────────────────────────────────────┐
    │              BUSINESS LOGIC LAYER                     │
    ├──────────────────────────────────────────────────────┤
    │  • Form validation                                    │
    │  • Permission checks                                  │
    │  • Intent matching                                    │
    │  • Keyword matching                                   │
    │  • Response formatting                                │
    └──────────────────────────────────────────────────────┘
                           ↓
    ┌──────────────────────────────────────────────────────┐
    │              DATA ACCESS LAYER                        │
    ├──────────────────────────────────────────────────────┤
    │  ChatbotTraining ORM                                  │
    │  • CRUD operations                                    │
    │  • Filtering & searching                              │
    │  • Ordering & pagination                              │
    └──────────────────────────────────────────────────────┘
                           ↓
    ┌──────────────────────────────────────────────────────┐
    │              DATABASE LAYER                           │
    ├──────────────────────────────────────────────────────┤
    │  SQLite Database                                       │
    │  Table: core_chatbottraining                          │
    │  ├─ question (TEXT)                                   │
    │  ├─ answer (TEXT)                                     │
    │  ├─ intent (VARCHAR)                                  │
    │  ├─ keywords (VARCHAR)                                │
    │  ├─ is_active (BOOLEAN)                               │
    │  ├─ created_at (DATETIME)                             │
    │  ├─ updated_at (DATETIME)                             │
    │  └─ created_by_id (FOREIGNKEY)                        │
    └──────────────────────────────────────────────────────┘
```

## Request/Response Flow

### Training Page Load
```
┌─────────┐
│  Admin  │
│  User   │
└────┬────┘
     │
     │ GET /chatbot/training/
     ↓
┌─────────────────────────────┐
│   Django URL Router         │
│   (core/urls.py)            │
└────────┬────────────────────┘
         │
         │ Match: path('chatbot/training/', ...)
         ↓
┌─────────────────────────────┐
│   chatbot_training View     │
│   (core/views.py)           │
│ 1. Check auth (login)       │
│ 2. Check permission (staff) │
│ 3. Get all training pairs   │
│ 4. Render template          │
└────────┬────────────────────┘
         │
         │ Returns ChatbotTraining.objects.all()
         ↓
┌─────────────────────────────┐
│   Database Query            │
│   SELECT * FROM             │
│   core_chatbottraining      │
│   ORDER BY -created_at      │
└────────┬────────────────────┘
         │
         │ Rows: [pair1, pair2, pair3, ...]
         ↓
┌─────────────────────────────┐
│   Template Rendering        │
│   chatbot_training.html     │
│ 1. Build form               │
│ 2. Loop pairs               │
│ 3. Create table rows        │
│ 4. Add search JS            │
│ 5. Style with CSS           │
└────────┬────────────────────┘
         │
         │ HTML Response
         ↓
┌─────────┐
│ Browser │ ← Renders page with form + table
└─────────┘
```

### Add Training Pair Flow
```
┌─────────────────────────────┐
│  Admin fills form            │
│  - Question                  │
│  - Answer                    │
│  - Intent                    │
│  - Keywords                  │
│  - Check "Active"            │
│  - Click "Save"              │
└────┬────────────────────────┘
     │
     │ POST /chatbot/training/
     ↓
┌─────────────────────────────┐
│  chatbot_training View      │
│  1. Parse POST data         │
│  2. Validate inputs         │
│  3. Create ChatbotTraining  │
│     object                  │
│  4. Auto-set created_by     │
│  5. Save to database        │
│  6. Create success message  │
└────┬────────────────────────┘
     │
     │ INSERT INTO core_chatbottraining
     │ (question, answer, intent, keywords,
     │  is_active, created_by_id, created_at)
     │ VALUES (...)
     ↓
┌─────────────────────────────┐
│  Database                   │
│  New row created            │
│  ID: auto-increment         │
└────┬────────────────────────┘
     │
     │ Redirect to /chatbot/training/
     ↓
┌─────────────────────────────┐
│  Show success message       │
│  "✅ Training pair added"    │
│  Reload page                │
└─────────────────────────────┘
```

### Chatbot Uses Training
```
┌────────────────┐
│  User Question │
│  "How to join  │
│   my team?"    │
└────┬───────────┘
     │
     │ POST /api/chatbot/
     ↓
┌──────────────────────────────────┐
│  chatbot_response() View         │
│  1. Receive question             │
│  2. Call chatbot.get_response()  │
└────┬─────────────────────────────┘
     │
     ↓
┌──────────────────────────────────┐
│  ChatBot.get_response()          │
│  1. understand_intent()          │
│  2. get_response()               │
└────┬─────────────────────────────┘
     │
     ↓
┌──────────────────────────────────┐
│  understand_intent()             │
│  [CHECK TRAINING FIRST]          │
│  For each training pair:         │
│    - Check if keywords match     │
│    - "join" in question?         │
│    - "team" in question?         │
└────┬─────────────────────────────┘
     │
     ├─→ YES: Return intent='team_guide'
     │        confidence=0.95
     │
     └─→ NO: Continue to next step
           (guide keywords, spaCy, etc.)
     │
     ↓
┌──────────────────────────────────┐
│  get_response()                  │
│  IF intent == 'team_guide':      │
│    - Query ChatbotTraining DB    │
│    - Find matching answer        │
│    - Format HTML response        │
│    - Return to user              │
└────┬─────────────────────────────┘
     │
     │ SELECT answer FROM core_chatbottraining
     │ WHERE keywords LIKE '%join%'
     │ AND keywords LIKE '%team%'
     │ AND is_active = True
     ↓
┌──────────────────────────────────┐
│  Format Response                 │
│  "1. Login to your account       │
│   2. Go to Team Management       │
│   3. Click 'Join Team'           │
│   4. Enter team code"            │
└────┬─────────────────────────────┘
     │
     │ JSON Response with HTML
     ↓
┌─────────────────┐
│  User sees:     │
│  [Training      │
│   Answer]       │
└─────────────────┘
```

## Database Interaction Diagram

```
┌────────────────────────────────────┐
│  Django ORM (Object-Relational)    │
│  ---------------------------------  │
│  ChatbotTraining Model             │
└────────┬─────────────────────────────┘
         │
    ┌────┴─────────────────────────┐
    │                              │
    ↓                              ↓
┌──────────────────┐     ┌──────────────────┐
│  Python Objects  │     │  Database Tables │
├──────────────────┤     ├──────────────────┤
│ pair = Chatbot   │     │ core_chatbot     │
│ Training.objects │     │ training         │
│ .get(id=1)       │     │ ──────────────── │
│                  │     │ id   | question  │
│ pair.question    │←───→│ 1    | "How...  │
│ pair.answer      │     │      | answer   │
│ pair.intent      │     │ 2    | "How...  │
│ pair.keywords    │     │      | ...      │
│ pair.is_active   │     │ 3    | ...      │
│ pair.created_at  │     │      | ...      │
└──────────────────┘     └──────────────────┘
     ↑      ↓                  ↑      ↓
     │      └──────┬───────────┘      │
     │             │                  │
     └─────────────┴──────────────────┘
       SQL Queries
```

## User Permission Flow

```
┌──────────────────────────────────┐
│  User Accesses /chatbot/training/│
└────┬─────────────────────────────┘
     │
     │ @login_required decorator
     ↓
┌──────────────────────────────────┐
│  Is user logged in?              │
├──────────────────────────────────┤
│  NO  → Redirect to /login/       │
│  YES → Continue                  │
└────┬─────────────────────────────┘
     │
     │ Check: if not request.user.is_staff
     ↓
┌──────────────────────────────────┐
│  Is user staff/superuser?        │
├──────────────────────────────────┤
│  NO  → Show error msg            │
│       → Redirect to /home/       │
│  YES → Load page                 │
└────┬─────────────────────────────┘
     │
     ↓
┌──────────────────────────────────┐
│  Admin Panel Loaded              │
│  - Show form                     │
│  - Show training table           │
│  - Enable edit/delete buttons    │
└──────────────────────────────────┘
```

## Intent Matching Priority

```
User Question: "How do I create a team?"
     │
     ↓
┌────────────────────────────────────────┐
│ Step 1: Check ChatbotTraining Table    │
├────────────────────────────────────────┤
│ SELECT * WHERE is_active = True        │
│                                         │
│ For each training pair:                │
│   keyword_list = ['team', 'create',... │
│   if any(kw in question):              │
│       match = True                     │
│       confidence = 0.95                │
│                                         │
│ "create" in question? YES ✓            │
│ "team" in question? YES ✓              │
│ → MATCH FOUND!                         │
│ → Return training answer               │
└────┬───────────────────────────────────┘
     │
     ↓ IF NO MATCH...
┌────────────────────────────────────────┐
│ Step 2: Check Guide Features           │
├────────────────────────────────────────┤
│ if 'create' in question:               │
│   if 'team' in question:               │
│     return team_guide                  │
└────┬───────────────────────────────────┘
     │
     ↓ IF NO MATCH...
┌────────────────────────────────────────┐
│ Step 3: spaCy Semantic Analysis        │
├────────────────────────────────────────┤
│ nlp = spacy.load()                     │
│ doc = nlp("How do I create a team?")   │
│ Find semantic meaning                  │
│ if similarity > threshold:             │
│   return semantic answer               │
└────┬───────────────────────────────────┘
     │
     ↓ IF NO MATCH...
┌────────────────────────────────────────┐
│ Step 4: General Information            │
├────────────────────────────────────────┤
│ Check events, timeline, help           │
│ if match: return info                  │
└────┬───────────────────────────────────┘
     │
     ↓ IF NO MATCH...
┌────────────────────────────────────────┐
│ Step 5: Help Message                   │
├────────────────────────────────────────┤
│ "I didn't understand. Try..."          │
└────────────────────────────────────────┘
```

## File Structure

```
ARTIFA FEST/
├── core/
│   ├── models.py
│   │   └── ChatbotTraining (NEW)
│   ├── views.py
│   │   ├── chatbot_training() (NEW)
│   │   ├── edit_training() (NEW)
│   │   └── delete_training() (NEW)
│   ├── urls.py
│   │   └── 4 new routes (NEW)
│   ├── admin.py
│   │   └── ChatbotTrainingAdmin (NEW)
│   ├── templates/core/
│   │   ├── chatbot_training.html (NEW)
│   │   └── chatbot_training_edit.html (NEW)
│   └── migrations/
│       └── 0015_chatbottraining.py (NEW)
│
├── db.sqlite3
│   └── core_chatbottraining table (NEW)
│
└── Documentation/
    ├── CHATBOT_TRAINING_SYSTEM_COMPLETE.md (NEW)
    ├── CHATBOT_TRAINING_GUIDE.md (NEW)
    ├── CHATBOT_TRAINING_QUICKSTART.md (NEW)
    ├── CHATBOT_TRAINING_HTML_REFERENCE.md (NEW)
    └── CHATBOT_TRAINING_IMPLEMENTATION_SUMMARY.md (NEW)
```

## Component Interaction

```
┌─────────────────────────────────┐
│      Web Browser                │
│  (Admin User Interface)          │
└────────┬────────────────────────┘
         │ HTTP GET/POST
         ↓
┌─────────────────────────────────┐
│   Django URL Router             │
│   (core/urls.py)                │
│                                 │
│  /chatbot/training/         ────→ chatbot_training
│  /chatbot/training/add/     ────→ chatbot_training
│  /chatbot/training/<id>/edit────→ edit_training
│  /chatbot/training/<id>/del ────→ delete_training
└────────┬────────────────────────┘
         │
         ↓
┌─────────────────────────────────┐
│   Django Views                  │
│   (core/views.py)               │
│                                 │
│   • Auth check                  │
│   • Permission check            │
│   • Business logic              │
│   • Template rendering          │
└────────┬────────────────────────┘
         │
         ↓
┌─────────────────────────────────┐
│   Django Models                 │
│   (core/models.py)              │
│                                 │
│   ChatbotTraining               │
│   • CRUD operations             │
│   • Validation                  │
│   • Relationships               │
└────────┬────────────────────────┘
         │
         ↓
┌─────────────────────────────────┐
│   Database                      │
│   (SQLite)                      │
│                                 │
│   core_chatbottraining table    │
│   • Store Q&A pairs             │
│   • Indexed fields              │
│   • Foreign keys                │
└────────┬────────────────────────┘
         │
         ↓
┌─────────────────────────────────┐
│   Response                      │
│   (HTML + CSS + JS)             │
│                                 │
│   • Form / Table                │
│   • Search functionality        │
│   • Success messages            │
└────────┬────────────────────────┘
         │ HTTP Response
         ↓
┌─────────────────────────────────┐
│      Web Browser                │
│  (Displays Training Page)       │
└─────────────────────────────────┘
```

## Security Checks

```
Request to /chatbot/training/
     │
     ├─→ Is user authenticated?
     │   NO → Redirect to /login/
     │   YES → Continue
     │
     ├─→ Is user.is_staff = True OR is_superuser?
     │   NO → Show error + redirect to /home/
     │   YES → Continue
     │
     ├─→ Valid CSRF token?
     │   NO → Reject request
     │   YES → Continue
     │
     ├─→ Is request.method valid?
     │   NO → Reject
     │   YES → Process
     │
     └─→ Are form fields valid?
         NO → Show validation errors
         YES → Save to database
```

## Database Query Examples

### Get All Training Pairs
```python
pairs = ChatbotTraining.objects.filter(is_active=True).order_by('-created_at')
# SQL: SELECT * FROM core_chatbottraining 
#      WHERE is_active = 1 
#      ORDER BY created_at DESC
```

### Search Training
```python
pairs = ChatbotTraining.objects.filter(
    Q(question__icontains=search_term) |
    Q(answer__icontains=search_term) |
    Q(intent__icontains=search_term)
)
# SQL: SELECT * FROM core_chatbottraining 
#      WHERE question LIKE '%term%'
#      OR answer LIKE '%term%'
#      OR intent LIKE '%term%'
```

### Get By Intent
```python
pairs = ChatbotTraining.objects.filter(intent='team_guide')
# SQL: SELECT * FROM core_chatbottraining 
#      WHERE intent = 'team_guide'
```

---

**Architecture Version:** 1.0
**Last Updated:** January 26, 2025
**Status:** ✅ Complete
